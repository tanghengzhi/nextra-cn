# 文档主题

import Callout from 'nextra-theme-docs/callout'

<Callout>本网站是使用文档主题构建的。</Callout>

[快速上手](/get-started)页面是一个很好的起点。

你只需要将所有 Markdown 文件（`.md`，`.mdx`）放在 `pages` 目录下即可。这是组织文档的最佳方式：

<br />

![](../../../public/demo.png)

您还可以使用 `meta.json` 文件来配置页面的顺序和显示名称（[示例](https://github.com/shuding/nextra/blob/master/pages/meta.json)）。
